# Random-Number-Guessing-Game
This project is a console-based Random Number Guessing Game. The program generates a random number between 1 and 100 using Python’s random module. The user is given 5 attempts to guess the correct number. If the guess is lower than the secret number, the program displays 'Too low!'. If the guess is higher than the secret number, the program display
